from django.apps import AppConfig


class IncidentReportingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.incident_reporting"
    verbose_name = 'Incident Management'
